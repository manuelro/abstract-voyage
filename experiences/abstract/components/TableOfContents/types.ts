export type { FigureItem, HeadingItem } from '../../../../helpers/articleOutline'

export type TableOfContentsPresentation = {
  textInk: string
  labelInk: string
  hoverInk: string
  hoverBackground: string
  hoverSettlingBackground: string
  activeInk: string
  activeBackground: string
  activeSettlingBackground: string
  mutedInk: string
}
